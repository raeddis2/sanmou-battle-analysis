# 兵力因子公式初步分析

日期：2026-06-22

## 当前结论

基于当前 SQLite 数据库 `data/sanmou_battles.sqlite` 的伤害上下文，攻击方当前兵力对伤害的影响暂按下面公式建模最稳：

```text
F_source(N) = 1                         , N >= 9000
F_source(N) = (N / 9000) ^ 0.38          , N < 9000
```

这里 `N` 是造成该次伤害时攻击方的当前兵力。`0.38` 不是最终定值，而是当前样本的拟合中心；把无衰减阈值从 `8000` 扫到 `10000`，拟合指数大致落在 `0.37 ~ 0.39`。

## 取数口径

- 唯一数据源：SQLite 的 `damage_contexts`、`participants`、`reports`。
- 攻击方当前兵力：优先读 `damage_contexts.source_context_json.hp`；为空时用该武将 `participants.initial_troops` 补齐。
- 目标当前兵力：使用 `damage_contexts.target_hp_before`；为空时从目标上下文/初始兵力补齐。
- 拟合样本只保留非致死伤害、双方兵力已知、且本次动作没有会心/奇谋等触发的记录。
- 分组时固定 report、攻方、目标、战法、动作类型、兵种、阵营、实时属性、激活 buff 等，尽量让组内只剩兵力变化。
- 为估计攻击方兵力因子，默认只使用目标当前兵力 `>= cap` 的重复组，避免目标低兵力可能带来的承伤项干扰。

## 当前拟合结果

默认命令：

```powershell
python scripts/analyze_troop_factor.py --fit-target
```

默认 `cap=9000`、排除明显内含变化倍率或复合机制的战法后：

```text
damage_samples: 3125
clean_samples: 1894
source factor: rows=58, groups=25, alpha=0.381012, rmse_log=0.007997, r2=0.938716
```

候选指数对比：

| alpha | rmse_log | median_abs_log | p80_abs_log |
| ---: | ---: | ---: | ---: |
| 0.300000 | 0.010404 | 0.004654 | 0.011323 |
| 0.333333 | 0.008905 | 0.003813 | 0.009227 |
| 0.350000 | 0.008393 | 0.003730 | 0.009605 |
| 0.380000 | 0.007997 | 0.003966 | 0.012315 |
| 0.400000 | 0.008148 | 0.005395 | 0.011286 |
| 0.450000 | 0.009801 | 0.005547 | 0.011348 |
| 0.500000 | 0.012629 | 0.006912 | 0.016662 |
| 0.544100 | 0.015602 | 0.008506 | 0.020878 |

按战法拆开看，当前样本不完全一致：

| 子样本 | rows | groups | alpha | 备注 |
| --- | ---: | ---: | ---: | --- |
| 普通攻击 | 4 | 2 | 0.449867 | 样本太少，只能参考 |
| 草船借箭 | 25 | 11 | 0.345842 | 谋略触发样本，拟合较稳定 |
| 纵马横枪 | 27 | 11 | 0.304994 | 追伤机制可能还有隐藏项 |
| 保守合并样本 | 58 | 25 | 0.381012 | 当前主口径 |

## 关于 0.5441 线索

知识表 `game_basic_rules` 中已有一条旧线索：`n^0.5441`，并说明 `n` 应理解为 `当前兵力 / 10000`，且 `9000` 以上无衰减。

当前数据库重复组不支持直接采用 `0.5441`：

- 在默认保守样本中，`alpha=0.5441` 的 `rmse_log=0.015602`，约为 `alpha=0.38` 的 1.95 倍。
- 如果用 `N/10000` 且 `9000` 以上截断，函数在 9000 附近存在跳变；当前拟合更适合写作 `(N / cap)^alpha`，其中 `cap` 约为 `9000`。

因此 `0.5441` 暂时保留为待复核线索，不作为当前公式结论。

## 防守方兵力因子

当前样本不能可靠确认目标当前兵力是否独立影响承伤。默认口径下尝试拟合目标兵力因子：

```text
target factor: rows=10, groups=5, alpha=-0.232781, rmse_log=0.072165, r2=0.128707
```

这个结果样本少、方向不稳定、解释力弱，暂不纳入公式。

## 后续验证方向

1. 优先补充同一攻防双方、同一战法、同一实时属性、目标兵力高于 9000、攻击方兵力跨多个区间的样本。
2. 单独设计普通攻击样本，因为当前普通攻击可用重复组只有 2 组。
3. 拆分战法的隐藏伤害段位，尤其是 `纵马横枪`、`文治武功`、`七进七出`、`千里突袭`。
4. 继续验证无衰减阈值到底是 `9000`、`10000`，还是依初始兵力/等级变化。
